# Bí mật — không bao giờ commit
.streamlit/secrets.toml
.env
*.key

# Python
__pycache__/
*.pyc
.venv/
venv/

# Artifact runtime (không commit)
streamlit.log
shot_*.png
scripts/sync-log.txt

# Claude local config
.claude/settings.local.json

# Bản gốc screenshot ở root (bản chuẩn nằm trong docs/screenshots/)
/final_*.png

# VPS shim env (không commit)
*.env
vps/*.env

# Live verify screenshots (artifact)
/live_*.png
shot_hermes_chat.png

# Local scratch directories
scratch/

